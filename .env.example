# Copy to .env (never commit .env)

# Finviz Elite → Settings → API → Export API token (CSV export only; NOT quote URL auth=)
FINVIZ_AUTH_KEY=

# Optional: DeepSeek for watchlist catalyst tags
DEEPSEEK_API_KEY=
